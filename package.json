{
  "name": "tokiponailo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "elm make src/Main.elm --output=dist/main.js && cp -r ./public/* ./dist && zip ./dist/dist.zip ./dist/*",
    "dev": "elm-live src/Main.elm --dir=./public -- --output=public/main.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm-live": "^4.0.2"
  }
}
